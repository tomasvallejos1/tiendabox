# TiendaBox Bruno Collection

Coleccion de Bruno para probar la API REST de TiendaBox.

Organizacion elegida:
- Una carpeta por recurso (`category` y `product`).
- Dentro de cada carpeta, requests separadas por caso de uso.
- Esta estructura evita mezclar CRUDs de distintos recursos y facilita encontrar cada endpoint rapido.
